# TASKS

Status legend: `⬜` not started, `⏳` in progress/blocked, `✅` done.

## Task Backlog
| Task ID | Status | Complexity | Detailed Description |
| --- | --- | --- | --- |
| COI-ONLINE-031 | ⏳ | 3 | Lock and publish the production 3D camera spec. Current `world_canvas_3d` camera constants are active but still provisional; missing final acceptance captures, explicit indoor/outdoor clamps, and documented pass/fail thresholds. |
| COI-ONLINE-033 | ⏳ | 5 | Finish production camera controller behavior: obstruction push-in/collision avoidance, zoom curve, and deterministic runtime camera profiles. Current path covers fixed ARPG pitch/yaw/follow but lacks full collision/occlusion handling. |
| COI-ONLINE-034 | ⏳ | 5 | Replace lightweight 3D actor movement with CharacterBody3D + combat-grade animation graph integration and server-sync friendly state transitions. Current implementation plays core clips but is not yet full production locomotion/combat controller architecture. |
| COI-ONLINE-035 | ⬜ | 4 | Extend backend/client world bootstrap contract for 3D-specific fields (`spawn_yaw`, camera profile key, map scale metadata, optional scene variant hints), including schema validation and backward-compatible client fallback behavior. |
| COI-ONLINE-036 | ⏳ | 4 | Complete staged 3D content packaging: formal scene bundle structure + manifest validation + rollout toggles. Runtime object-domain ingestion exists, but release-time package governance and bundle contract are incomplete. |
| COI-ONLINE-039 | ⏳ | 5 | Deliver production-quality 3D Sellsword templates (male/female) beyond baseline concept-driven exports: higher-detail topology/material polish, finalized rig/weights, and expanded clip set (including sit variants). |
| COI-ONLINE-042 | ⏳ | 4 | Formalize Blender->Godot import contract: import presets, material remap policy, retarget mapping, and collision generation validation. Runtime GLB loading works, but import governance is not yet fully codified. |
| COI-ONLINE-043 | ⏳ | 5 | Continue Level Designer 3D migration. Current editor supports object JSON + spawn yaw/z, but terrain paint, placement gizmos, navmesh bake triggers, and transition-volume authoring still need implementation. |
| COI-ONLINE-044 | ⏳ | 4 | Continue Asset Designer 3D migration. Current tooling has bootstrap template/domain payload support, but complete 3D card UX + metadata diff/validation workflow remains unfinished. |
| COI-ONLINE-045 | ⏳ | 3 | Expand starter 3D environment kit from current ground/grass/tree baseline into a reusable biome pack (rock/cliff/prop variants + documentation + ingest checks). |
| COI-ONLINE-046 | ⬜ | 4 | Implement 3D performance budget guardrails: LOD switching distances, impostor/culled foliage strategy, shadow cascade tuning, material complexity limits, and target frame-time budgets on baseline hardware. Add telemetry counters for draw calls, triangles, and frame timing snapshots. |
| COI-ONLINE-047 | ⏳ | 3 | Expand 3D migration verification. UI regression + 3D runtime contract checks are live, but world-entry smoke tests on packaged runtime and backend bootstrap compatibility tests for new 3D fields are still pending. |
| COI-ONLINE-048 | ⬜ | 2 | Create phased rollout/migration governance task: implementation sequence, feature flags per stage, rollback criteria, and go/no-go gates for replacing 2D runtime path with 3D default. Include explicit dependency map linking tasks 031-047 and acceptance checkpoints per phase. |
| COI-ONLINE-022 | ⬜ | 3 | Optional art-tool integration backlog item: formalize external artist workflow for `Aseprite`/`Krita` and import automation into the repo pipeline. Scope includes (1) canonical folder/naming/export conventions for layered source files and runtime sprite sheets, (2) optional CLI export wrappers where tooling is available locally, (3) validation script updates to fail on malformed/partial packs, and (4) concise contributor documentation for handoff between art authoring and runtime ingest. Deferred until active art production requires it. |

## Completed Tasks
| Task ID | Status | Complexity | Detailed Description |
| --- | --- | --- | --- |
| COI-ONLINE-059 | ✅ | 2 | Reconciled stale 3D migration backlog entries by marking partially delivered tasks as `⏳`, moving superseded duplicates (`032`, `037`, `038`, `040`, `041`) into the archived/superseded section, and aligning remaining open scope with already shipped slices (`049-058`). |
| COI-ONLINE-058 | ✅ | 3 | Re-themed the in-game shell UI to align with the new concept-art direction: replaced the old fantasy-city background image with a procedural steel/leather backdrop, switched to bundled serif display/body fonts (`Cinzel`, `Cormorant Garamond`), and updated tokenized panel/button/border colors to match grounded armor-and-leather visual language. |
| COI-ONLINE-057 | ✅ | 3 | Integrated concept-art-driven Sellsword pass: `tools/blender/scripts/generate_sellsword_3d_assets.py` now loads `concept_art/sellsword_front.png` + `concept_art/sellsword_back.png` as modeling references, and regenerated runtime GLBs (`assets/3d/generated/sellsword_male.glb`, `sellsword_female.glb`, plus ground/foliage exports) so the in-game 3D actor path uses Blender-exported assets instead of procedural-only placeholders. |
| COI-ONLINE-056 | ✅ | 4 | Hardened 3D pipeline verification in CI: added `game-client/tests/check_3d_runtime_contract.py`, wired it into `.github/workflows/release.yml`, expanded UI regression hashing inputs to include 3D runtime/preview/model scripts, and removed markdown path-ignore in release workflow so docs-driven changes also execute packaging validation. |
| COI-ONLINE-055 | ✅ | 4 | Upgraded 3D character presentation baseline: `sellsword_3d_factory.gd` now enforces a standard animation contract (`idle/walk/run/attack/cast/hurt/death`) and preview/world playback integration; account/world camera lighting and animation-state handling were aligned to remove floating/bob conflicts and improve in-world readability. |
| COI-ONLINE-054 | ✅ | 4 | Completed Blender automation reliability pass for headless generation on Linux workstations: installer now handles protected download hosts with fallback clients and versioned base URL support; headless runner now injects local shared-lib paths (`.tools/blender/libs`) to resolve missing runtime dependencies such as `libxkbcommon.so.0`. |
| COI-ONLINE-053 | ✅ | 5 | Delivered 3D runtime parity + level-object flow: `world_canvas_3d.gd` now consumes object placement payloads as authoritative scene input, supports runtime asset-domain scene mapping, registers collision blockers, emits transition triggers, generates baseline navmesh region, and honors spawn marker yaw/z for deterministic world entry behavior. |
| COI-ONLINE-052 | ✅ | 4 | Delivered first 3D tooling/editor integration slice: level editor now supports `Objects JSON (3D placements)` plus spawn yaw/z controls with automatic `spawn_marker_3d` upsert on draft save; asset editor includes a one-click 3D domain template payload; and starter 3D environment scenes were added (`ground_tile_stone_3d`, `foliage_grass_a_3d`, `foliage_tree_dead_3d`) for early level blockout. |
| COI-ONLINE-051 | ✅ | 4 | Added programmatic Blender automation bootstrap: pinned installer (`tools/blender/install_blender.py`), headless runner (`tools/blender/run_blender_headless.py`), and baseline Blender generation script (`tools/blender/scripts/generate_sellsword_3d_assets.py`) that exports male/female sellsword and ground/foliage GLB placeholders. |
| COI-ONLINE-050 | ✅ | 4 | Replaced account hub character previews with 3D podium widgets (`character_podium_preview_3d.gd`) while preserving existing UX contract (primary + inset previews, facing controls, drag rotate) and fixed empty-list behavior by adding explicit preview clear path so no placeholder character renders when list is empty. |
| COI-ONLINE-049 | ✅ | 4 | Established initial Godot 3D runtime scaffold (`world_canvas_3d.gd`) with PoE-inspired angled camera constants, player movement/facing, starter ground+foliage population, and runtime renderer selection (`2d`/`3d`) in `client_shell.gd`; account actions are now selection-gated (`Play/Delete` disabled until a character is selected). |
| COI-ONLINE-030 | ✅ | 5 | Delivered Sellsword fidelity v3 generator pass: rebuilt procedural model drawing with smoother anti-aliased rendering, stronger material shading, and explicit front/side/back directional silhouettes (including away-facing rows), while keeping runtime sheet contract at `640x640`; regenerated all male/female animation sheets and layered source slices. |
| COI-ONLINE-029 | ✅ | 3 | Finalized account-hub flow fixes from follow-up feedback: character list stays the default view even for empty accounts (no back-to-create loop), create action no longer shows a confirmation popup, create preview title headline is removed, and client now logs character-list API diagnostics (`rows` count + failure status/message) to validate frontend/backend `/characters` connectivity by account. |
| COI-ONLINE-028 | ✅ | 2 | Synced `assets/iso_asset_manifest.json` Sellsword idle registry entries to the current `640x640` generated sheets (`*_640` keys/paths, `5120x5120` sheet dimensions) so release-time asset ingest validation succeeds after fidelity-v2 generation changes. |
| COI-ONLINE-026 | ✅ | 4 | Implemented account-hub UX polish pass from live feedback: removed manual list refresh action in favor of auto-refresh on view transitions, eliminated duplicate create buttons in list flow, hid left list sidebar during create flow, converted top-right settings cog into rectangular `Menu` button, moved create-flow back action to right sidebar footer, reordered create fields (`Character Name`, `Character Type`, `Sex`, `Character Type Lore`), removed starter-skills line from create lore block, and removed input field titles for name/type/sex. |
| COI-ONLINE-027 | ✅ | 5 | Implemented preview grounding + sprite fidelity v2: added podium grounding anchor/contact shadow/floor strip to prevent floating in large preview, added stronger world-scale inset backdrop/border for readability, upgraded procedural sellsword generator detail pass (armor/cloth/hair/face shading and materials), and increased generated source frame fidelity to `640x640` while preserving runtime world downscale behavior. |
| COI-ONLINE-001 | ✅ | 3 | Pivot baseline established: renamed project/product surfaces to Children of Ikphelion (user-facing), reactivated Godot online shell entry (`client_shell.gd`), preserved isometric world runtime, and introduced backend file-based runtime gameplay config endpoint (`/content/runtime-config`) with client-side fallback to `/content/bootstrap`. |
| COI-ONLINE-002 | ✅ | 4 | Added authenticated request recovery in Godot shell: automatic `/auth/refresh` retry for protected `401` responses, single retry of original request, and safe session reset to auth screen when refresh fails. |
| COI-ONLINE-003 | ✅ | 4 | Added backend world bootstrap contract endpoint (`/characters/{id}/world-bootstrap`) and client play-flow integration so world entry is assembled from one server-authored payload (character snapshot, level payload, spawn coordinates, runtime config descriptor/domains, version policy, instance assignment). |
| COI-ONLINE-004 | ✅ | 5 | Implemented party service v1 (`/party`) with create/invite/accept/decline/leave/kick/promote-owner and party-aware instance routing for world entry. |
| COI-ONLINE-005 | ✅ | 5 | Implemented hub-scoped websocket presence behavior: zone presence broadcasts limited to hub levels and deduplicated hub join/leave fanout tracking in realtime hub state. |
| COI-ONLINE-006 | ✅ | 4 | Implemented instance lifecycle manager with deterministic instance IDs (`solo/party/hub`), session assignment persistence, expiration handling, reconnect-aware restoration hints, and `/instances/current` + `/instances/heartbeat`. |
| COI-ONLINE-007 | ✅ | 4 | Added server-authoritative gameplay action resolver (`/gameplay/resolve-action`) with skill validation, XP/level progression updates, loot grants, and persistence of authoritative location/progression changes. |
| COI-ONLINE-008 | ✅ | 3 | Hardened runtime gameplay config service with schema/domain validation, explicit schema+version metadata, signature pin enforcement, staged publish, active publish, and rollback endpoints under `/content/runtime-config/*`. |
| COI-ONLINE-009 | ✅ | 3 | Added runtime gameplay config cache with signature verification in Godot client (`runtime_gameplay_cache.json`) and safe fallback when runtime-config endpoint is unavailable. |
| COI-ONLINE-010 | ✅ | 3 | Added dedicated `Security Scan` workflow and expanded backend deploy workflow with post-deploy smoke coverage (`health/deep` + online auth/character/gameplay loop smoke). |
| COI-ONLINE-011 | ✅ | 3 | Continued naming migration and compatibility pass for Children of Ikphelion across backend defaults/scripts and build metadata while preserving compatibility where required for existing installs. |
| COI-ONLINE-012 | ✅ | 4 | Consolidated admin-only boundaries for designer/config publish surfaces (level/content/runtime-config controls) with existing grace/forced-logout publish-drain enforcement. |
| COI-ONLINE-013 | ✅ | 3 | Expanded observability with auth success/failure counters, websocket disconnect reason tracking, instance assignment/restore counters, and instance occupancy metrics exposed via ops metrics endpoint. |
| COI-ONLINE-014 | ✅ | 4 | Added end-to-end online smoke harness (`backend/scripts/smoke_online_loop.py`) for register/login/MFA toggle/create/bootstrap/location/gameplay authority flow validation. |
| COI-ONLINE-015 | ✅ | 4 | Updated updater UX messaging to show build/content release context and clearer forced-update/content-mismatch reasons in client-side user-facing error/status copy. |
| COI-ONLINE-016 | ✅ | 5 | Implemented anti-cheat trust-boundary v1 on server action ingestion: movement sanity checks, action rate guardrails, action nonce replay protection, and security-event hook emission. |
| COI-ONLINE-017 | ✅ | 3 | Added Steam dual-distribution implementation plan document (`docs/STEAM_DUAL_DISTRIBUTION.md`) while keeping standalone launcher/update flow intact. |
| COI-ONLINE-018 | ✅ | 3 | Restored backend deploy CI workflow (`deploy-backend.yml`) with backend-only change filtering and Cloud Run deploy path separated from launcher/game release workflow. |
| COI-ONLINE-019 | ✅ | 3 | Authored initial hard character preset catalog in runtime gameplay config and then consolidated it to a single production preset (`sellsword`) with male/female selection handled through `appearance_key`. |
| COI-ONLINE-020 | ✅ | 3 | Wired curated preset selection UX in Godot character creation: preset dropdown, preset-to-fields/stat/skill/budget application, preview sync, and `preset_key` posting to backend character create API. |
| COI-ONLINE-021 | ✅ | 4 | Implemented Sellsword V1 character art pipeline: generated 96x96 layered-ready male/female sprite sheets (8 directions; idle/walk/run/attack/cast/hurt/death/sit-crossed/sit-kneel), added runtime catalog metadata, and wired animated directional preview + world actor rendering. |
| COI-ONLINE-023 | ✅ | 4 | Account-hub UI regression recovery: replaced unstable split-container composition with fixed side-navigation + dedicated list/create view containers, restored centered non-world title behavior, and reinstated themed confirmation dialogs for character create/delete actions. |
| COI-ONLINE-024 | ✅ | 3 | Added dual preview for list/create flows: large podium preview plus inset world-scale mirror preview, with rotation/facing synchronization from primary preview drag/arrow controls. |
| COI-ONLINE-025 | ✅ | 4 | Upgraded Sellsword pipeline to 4x source fidelity (384 frame size), regenerated catalog/runtime sheets, and updated in-world actor rendering to downscale source frames to gameplay draw size for stable readability/performance. |

## Archived / Superseded
| Task ID | Status | Complexity | Detailed Description |
| --- | --- | --- | --- |
| COI-ONLINE-032 | ✅ | 0 | Superseded by delivered implementation slices `COI-ONLINE-049` and `COI-ONLINE-053` (3D runtime foundation + parity hooks). |
| COI-ONLINE-037 | ✅ | 0 | Superseded by delivered implementation slice `COI-ONLINE-050` (3D podium preview replacement). |
| COI-ONLINE-038 | ✅ | 0 | Superseded by delivered implementation slices `COI-ONLINE-049` and follow-up account-flow fixes (selection-gated actions + empty-state preview clearing). |
| COI-ONLINE-040 | ✅ | 0 | Superseded by delivered implementation slices `COI-ONLINE-051` and `COI-ONLINE-054` (Blender toolchain bootstrap + reliability pass). |
| COI-ONLINE-041 | ✅ | 0 | Superseded by delivered implementation slices `COI-ONLINE-051` and `COI-ONLINE-057` (headless Blender generation + concept-driven asset exports). |
| GOK-SPRPG-* | ✅ | 0 | Superseded by Children of Ikphelion online ARPG pivot; retained only as historical implementation record. |
| GOK-MMO-* | ✅ | 0 | Legacy MMO task stream is superseded and retained only for traceability. |
