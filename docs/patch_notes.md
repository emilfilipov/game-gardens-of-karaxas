- Completed `GOK-MMO-236`: made Godot host settings deployable by adding packaged `runtime_host.properties`, wiring release workflow defaults (`KARAXAS_RUNTIME_HOST`, `KARAXAS_GODOT_EXECUTABLE`, `KARAXAS_GODOT_PROJECT_PATH`), copying `game-client/` into payload, and resolving launcher runtime host settings with precedence env > packaged config > defaults.
